# Invoice Email Configuration
# The email address where invoices will be sent
INVOICE_EMAIL=billing@netzerholdings.com

# SMTP Configuration (for sending emails)
# For Gmail: smtp.gmail.com, port 587
# For Outlook: smtp-mail.outlook.com, port 587
# For custom SMTP: your-smtp-server.com
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# SMTP Authentication
# For Gmail: use your Gmail address and an App Password (not your regular password)
# Generate App Password: https://myaccount.google.com/apppasswords
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Note: If SMTP credentials are not configured, invoices will be logged to console
# in development mode instead of being emailed.

